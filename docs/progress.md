# Progress

Update this after finishing each milestone: what got built, what's still open, what to do next.

## Milestones

- [ ] 1. Project setup — Laravel + PostgreSQL + Inertia + Vue 3 + Tailwind, Laravel Boost installed, `.env` not committed
- [ ] 2. Auth & roles — Breeze login, `users`/`units` tables, role-based Policies
- [ ] 3. Document registration + QR — `documents`/`document_types` tables, registration form, QR generation
- [ ] 4. Workflow/routing — `document_movements` table, status transitions, assignment enforcement
- [ ] 5. Notifications — `notifications` table, scheduled aging check, email sending via queue
- [ ] 6. Dashboard — status counts, aging list, role-scoped visibility
- [ ] 7. Security hardening pass — rate limiting, `$fillable` audit, local asset bundling, backups
- [ ] 8. Test coverage pass — feature tests for auth, workflow transitions, assignment enforcement, notification triggers

## Log

_(empty — first entry goes here once milestone 1 is done)_
